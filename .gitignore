node_modules/
.env
logs/
settings.json
.playwright-mcp/

# Crashpad drops this when an embedded Chromium process (e.g. Playwright,
# Chrome DevTools MCP) can't reach its reporting pipe. Not ours.
debug.log

# Per-machine Claude Code overrides (shared config belongs in settings.json, not .local.json).
.claude/settings.local.json

# Generated by install.ps1 from assets/icon/*.png — PNGs are the source of truth.
assets/sublight.ico
